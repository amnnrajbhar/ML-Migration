import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { SharedmoduleModule } from '../shared/sharedmodule/sharedmodule.module';
import { WorkingCalendarRoutingModule } from './WorkingCalendar-routing.module';
import { WorkingCalendarComponent } from './WorkingCalendar.component';
import { CalendarModule, DateAdapter } from 'angular-calendar';
import { adapterFactory } from 'angular-calendar/date-adapters/date-fns';

@NgModule({
  imports: [
    CommonModule,
    FormsModule,
    SharedmoduleModule,
    CalendarModule.forRoot({
      provide: DateAdapter,
      useFactory: adapterFactory
    }),
    WorkingCalendarRoutingModule
  ],
  declarations: [WorkingCalendarComponent]
})
export class WorkingCalendarModule { }
